import { StudentsearchPipe } from './studentsearch.pipe';

describe('StudentsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
